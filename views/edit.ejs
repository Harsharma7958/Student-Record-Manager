<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" rel="stylesheet" />
</head>

<body>
    <div class="container min-w-[100vw] min-h-[100vh] font-[sans]">
        <nav class="bg-[#F9F5F6] w-full py-5 px-5 sm:px-20 flex justify-between items-center">
            <h1 class="font-[cursive] text-2xl sm:text-3xl font-bold"><a href="/">SRM</a></h1>
            <ul class="flex gap-3 md:gap-5 text-md lg:text-lg font-semibold ">
                <li><a href="/"
                        class="hover:border-b-2 hover:border-blue-500 hover:text-blue-500 duration-300 ease">Home</a>
                </li>
                <li><a href="/add"
                        class="hover:border-b-2 hover:border-blue-500 hover:text-blue-500 duration-300 ease">Add
                        Student</a></li>
                <li><a href="/allstudents"
                        class="hover:border-b-2 hover:border-blue-500 hover:text-blue-500 duration-300 ease">All
                        Students</a></li>
            </ul>
        </nav>
        <main class="w-full py-5 px-5 sm:px-20 flex flex-col items-center justify-center ">
            <h1 class="text-2xl sm:text-4xl text-blue-500 border-b-2 border-blue-500 font-bold">Update Student</h1>
            <form action="/update/<%= editedStudent._id %>" method="post" class="mt-10 flex flex-col gap-7 p-5">
                <div class="border-b-2 border-blue-500">
                    <i class="ri-user-fill text-blue-500 "></i>
                    <input type="text" placeholder="Name" name="name" class="bg-transperant text-black outline-none"
                        value="<%= editedStudent.name %>">
                </div>
                <div class="border-b-2 border-blue-500">
                    <i class="ri-booklet-fill text-blue-500"></i>
                    <select name="classdropdown" id="classdropdown" class="outline-none">
                        <option value="Select Class">Select Class</option>
                        <% ['Class I', 'Class II' , 'Class III' , 'Class IV' , 'Class V' , 'Class VI' , 'Class VII' , 'Class VIII' , 'Class IX' , 'Class X' , 'Class XI' , 'Class XII' ].forEach((e)=>{ %>
                            <% if(e === editedStudent.class){ %>
                            <option selected value="<%= e %>"><%= e %></option>
                            <% }else{ %>
                                <option value="<%= e %>"><%= e %></option>
                            <% } %> 
                           <% }) %>
                    </select>
                </div>
                <div class="border-b-2 border-blue-500">
                    <i class="ri-id-card-fill text-blue-500"></i>
                    <input type="text" placeholder="Roll No." name="rollno" value="<%= editedStudent.rollno %>"
                        class="bg-transperant text-black outline-none">
                </div>
                <input type="submit" value="Update"
                    class="bg-green-500 self-center px-4 py-2 rounded-full text-lg font-bold text-white hover:bg-green-700 duration-300">
            </form>
        </main>
    </div>
</body>

</html>